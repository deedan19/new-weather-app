// import 'package:flutter/material.dart';
// import 'package:intl/intl.dart';
//
// String timeInTheDay(TimeOfDay timeOfDay) {
//   DateFormat dateFormat = DateFormat("yyyy-MM-dd HH:mm:ss");
//   // String tod = dateFormat.format(timeOfDay);
//   final tod = Date
//   final format = DateTime.jm();
//   return format.format();
// }
//
// DateFormat dateFormat = DateFormat("yyyy-MM-dd HH:mm:ss");
// String string =
//     dateFormat.format(DateTime.now()); //Converting DateTime object to String
//
// DateTime dateTime = dateFormat.parse("2019-07-19 8:40:23");
//
// String formatTimeOfDay(TimeOfDay tod) {
//   // final now = DateTime.fromMicrosecondsSinceEpoch();
//   final dt = DateTime(now.year, now.month, now.day, tod.hour, tod.minute);
//   final format = DateFormat.jm(); //"6:00 AM"
//   return format.format(dt);
// }
